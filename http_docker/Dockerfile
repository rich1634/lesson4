# Используем легкий базовый образ alpine с установленным Python
FROM python:3.9-alpine

# Устанавливаем рабочую директорию внутри контейнера
WORKDIR /app

# Копируем текущий каталог (с вашим скриптом) в контейнер
COPY . /app

# Запускаем простой HTTP-сервер на порту 8000
CMD ["python", "-m", "http.server", "8000"]

# Открываем порт 8000 для доступа извне
EXPOSE 8000

